# Feature: fullscreen-live-header

## Objective
Design A for the fullscreen (>=140 cols) Gentle Shell layout: a one-row live header above the transcript+rail hstack carries everything that ticks per frame (context gauge, cost) plus the session identity (brand, cwd, branch, dirty, model · effort · profile); the right rail keeps only event-driven cards (Project, Changes, TODO). Agents leave the rail in every mode and stay in the above-editor widget, with metadata in fixed right-aligned columns. Sidebar cards (Status, Todos) switch from the champagne INFO frame to the rose palette (frame `border`, title `accent`). `working…` stays in the prompt title next to the flower; the header never shows it.

Design reference: https://claude.ai/artifact/Fg1oGGRT2e4ZUGY4Ktc6uL (page "Diseño A").

## Problem / Why
- `prepare()` in lib/shell-sidebar-layout.ts re-renders every rail section when any digest changes, and the footer digest (`JSON.stringify(footerModel())`) changes on every context/cost tick, so the whole 50-col rail re-renders and pi-tui's alt screen rewrites every shared row (`tui-alt-screen.js:1473` diffs whole rows).
- Info density is capped by the fixed `RAIL_WIDTH = 50` and single vertical stacking; live counters compete with static identity for the same column.
- The user prefers the rose frame palette over the champagne `CARD_TONE.INFO` frame for sidebar cards.

## Scope
In: fullscreen layout only (`host.mode === "fullscreen" && width >= SIDEBAR_BREAKPOINT`). Header row, Status card slimmed to Project/Changes, sidebar card palette, per-section render memo in `prepare()`, tests, README note.
Out: narrow (<140) bottom bar behaviour, per-model usage table (stays in `/gentle:usage`), warning/error card tones, prompt/petal animation, Pi internals beyond the existing NODE replacement.

## Constraints
- TDD strict (source: CLAUDE.md "Strict TDD Mode: enabled"). Runner: `node --experimental-strip-types --test tests/<file>.test.ts`; full: `pnpm test`; types: `pnpm run typecheck`.
- Conventional Commits, one work-unit commit per task with tests alongside; no AI attribution.
- Header and rails are pi-tui `Component`s composed via the existing `NODE` replacement; use a `vstack` `[header (basis 1, grow 0), hstack(left, rail)]` — `StackLayoutNode` supports `"vstack" | "hstack"` (layout-node.d.ts).
- Artifact language: English for code, comments, tests, docs.
- ~400 authored changed lines per task is a planning heuristic only.

## Tasks
- [x] T1 — Sidebar card palette: add a `CARD_TONE.PANEL` (or split title/frame roles) so `renderCard` can paint frame with role `border` and title with role `accent`; use it for the Status card (lib/shell-bar.ts:230) and the Todos/Agents sidebar cards. Warning/error/success tones unchanged. Tests in tests/shell-card.test.ts + call-site tests.
- [x] T2 — Live header row: `renderShellHeaderBar(model, theme, width): string` in lib/shell-bar.ts (left: `✿ Gentle Shell ⟡ cwd branch ±dirty ⟡ model · effort · profile`; right-aligned: `ctx <gauge> NN% ⟡ $cost`; degrade left segments first when width is short, same rules as the compact bar). Install it as a fullscreen header component in lib/shell-sidebar-layout.ts: `vstack` of header (1 row, own digest = JSON of the header model) over the existing hstack; the header replaces `renderSidebarBanner` in the rail (banner removed from rail when header is active). Wire in extensions/gentle-shell.ts via a new `sidebarHeader(tui, rail)` or an extension of `sidebarPart`. Status card drops Model/Effort/Context/Cost/Usage groups and keeps Project (cwd, branch, session, profile) + Changes + Integrations. Tests: tests/shell-bar.test.ts, tests/shell-sidebar-layout.test.ts, tests/gentle-shell.test.ts.
- [x] T3 — Per-section render memo: `prepare()` caches each section's rendered lines keyed by (section digest, revision-of-that-part, contentWidth, theme) so a header/Status digest change does not re-render Agents/TODO. Keep the throwing-digest degradation. Tests in tests/shell-sidebar-layout.test.ts proving Agents/TODO `render` is not called when only the footer digest changes.
- [x] T5 — Agents out of the rail: in extensions/gentle-agents.ts stop registering the `agents` sidebar part (`sidebarPart(tui, "agents", ...)`); the above-editor widget is the only Agents surface in every mode, so in fullscreen it must stay visible (verify the fullscreen layout keeps the bottom widgets — `sidebarPart` suppresses the bottom component only for registered parts). In lib/agents-widget.ts, lay the metadata out as fixed right-aligned columns — `model · effort | tokens | cost | elapsed` — each column sized to the widest value among shown tasks and right-aligned, so fields line up vertically across rows (today `meta()` joins one string per row and only the whole block is right-padded, see tests/agents-widget.test.ts:69-70). Queued rows fill only the elapsed column with `queued`. Keep the narrow-width degradation (drop task column, then fullMetrics) and the `allowMetadataRow` path. Update tests/agents-widget.test.ts and tests/gentle-agents.test.ts.
- [x] T6 — Rose frame for every INFO card: `CARD_TONE.INFO` paints frame `border` + title `accent` (same look as PANEL) so the review preflight card (extensions/gentle-shell.ts), the Agents widget in its quiet state (lib/agents-widget.ts `tone()` INFO) and any other INFO card match the rose theme. WARNING/ERROR/SUCCESS frames keep their tone colors (user decision). If INFO and PANEL end up identical, collapse PANEL into INFO and delete the extra tone. Tests: tests/shell-card.test.ts and affected call-site tests.
- [x] T7 — Reclaim the empty footer row in fullscreen: Pi's dock (`@earendil-works/pi-coding-agent/dist/modes/interactive/chat-viewport.js:18`) lists the footer entry as `{ component: footer, shrink: 1, minSize: 1 }`, so one blank row stays reserved under the editor while our suppressed footer renders `[]`. In lib/shell-sidebar-layout.ts, when the sidebar is active, have the root NODE replacement rebuild the dock entry so the footer's `minSize` is 0 (walk `original.call(root).entries`, find the dock VStack via its NODE, return a copy of its node with the footer entry's minSize set to 0 — never mutate Pi's objects). When the sidebar is inactive the original node is returned untouched. Test: a fake root whose dock carries a footer entry with minSize 1 yields minSize 0 only while active.
- [ ] T8 — Usage in the header, with shortcut and click: (a) `usageShortcut(env)` in extensions/gentle-shell.ts, default `alt+u`, env `GENTLE_PI_SHELL_USAGE_KEY` (empty/`off` disables), same shape as `changesShortcut`; extract the `/gentle:usage` handler body into `openUsage(ctx)` and register `pi.registerShortcut` to it. Verify `alt+u` is not already bound by gentle-agents/gentle-todo defaults. (b) `renderShellHeaderBar` gains a right-group segment after cost: `usage <gauge> NN% · <shortcut>` for the active provider's usage window (reuse `sidebarUsageLines`/`paintGauge` helpers; when no usage data: `usage · <shortcut>`; when the shortcut is disabled: no hint). It degrades after profile and before ctx/cost when width is short. The renderer also returns the usage segment's column span (`{ start, end }`) so a click can be resolved. (c) The header rail exposes `handleMouse`: a click whose x falls inside the usage span calls `openUsage(ctx)`; other header clicks are ignored. Wire pi-tui mouse dispatch to the header leaf in lib/shell-sidebar-layout.ts (verify how tui-alt-screen.js routes mouse to layout components; the header is a vstack entry, not inside the rail ScrollView). Tests: tests/shell-bar.test.ts (segment text, span, degradation), tests/gentle-shell.test.ts (shortcut env parsing, click opens usage), tests/shell-sidebar-layout.test.ts (mouse routed to header). Commit `feat(shell): usage segment with shortcut and click in the header`.
- [ ] T4 — Docs: README section for the fullscreen layout (header row, rail contents, palette). Commit with T3 or separately.

## Acceptance criteria
- Every INFO card (sidebar Status/Todos, review preflight, quiet Agents widget) paints frame `border` + title `accent`; warning/error/success cards unchanged.
- At >=140 cols fullscreen: first row is the header; rail shows Project, Changes, TODO cards; the Agents widget stays above the editor with `border` frame + `accent` title; no `working…` in the header; banner not duplicated.
- <140 cols: unchanged bottom bar.
- Fullscreen: no blank row reserved below the editor when the sidebar is active.
- Header shows `usage <gauge> NN% · alt+u`; alt+u and a click on that segment open the usage panel.
- A context/cost change re-renders only the header (and nothing in TODO).
- Agents metadata columns line up vertically across rows regardless of value widths.
- `pnpm test` and `pnpm run typecheck` pass (known unrelated failures, if any, listed below).

## Delivery
Strategy: ask-on-risk (default). Forecast ≈ 450–600 authored changed lines across T1–T4 → likely one PR slice; ask if the running count exceeds ~400 before the next commit.
Delivery budget boundary: branch point `0c844e8f` on main.

## Progress / Evidence
- T1 (sha: pending commit) — added `CARD_TONE.PANEL` (frame role `border`, title role `accent`) in lib/shell-card.ts; wired it into the Status card (lib/shell-bar.ts), the Todos sidebar rail (lib/shell-todo.ts, `options.scrollable`), and the Agents sidebar rail (lib/agents-widget.ts, new `options.panel`), leaving warning/error/success tones and the above-editor widgets on their existing tones.
  - RED: `AssertionError [ERR_ASSERTION]: The input did not match the regular expression /^<border>╭<\/border>.../` (tests/shell-card.test.ts, then repeated per call-site test in shell-bar/shell-todo/agents-widget).
  - GREEN: `node --experimental-strip-types --test tests/shell-card.test.ts tests/shell-bar.test.ts tests/shell-todo.test.ts tests/agents-widget.test.ts tests/gentle-shell.test.ts tests/gentle-todo.test.ts tests/gentle-agents.test.ts` → 217/217 pass.
  - `pnpm run typecheck` → no regressions (197 recorded diagnostics, 2 improved).

- T1 addendum (sha: 0e8e2021) — reverted the Agents sidebar `panel` tone option after the user's mid-task scope change: Agents is leaving the rail entirely in T5, so PANEL stays scoped to Status and Todos only.
- T2 (sha: pending commit) — `buildShellHeaderModel`/`renderShellHeaderBar` in lib/shell-bar.ts (brand ⟡ location ⟡ model·effort·profile, right-aligned ctx gauge + cost; degrades profile→effort→location→right-group→""); `sidebarHeader(tui, rail)` in lib/shell-sidebar.ts; `installSidebar` in lib/shell-sidebar-layout.ts now wraps the existing `[left, scroll]` hstack in a `vstack [header(basis 1), hstack(basis 0, grow 1)]` whenever a "header" part is registered and has non-blank content, removing the banner from the rail in that case (hosts without a registered header keep the exact old hstack-direct shape and banner, a deliberate backward-compatible fallback); wired in extensions/gentle-shell.ts's `setFooter` callback via `sidebarHeader`, disposed alongside the footer part and `installSidebar`'s uninstall. Status card (`renderShellSidebarBar`) drops Model/Effort/Context/Cost/Usage, keeping Project(cwd/branch/session/profile) + Changes + Integrations; the now-dead `sidebarUsageLines`/`consumedNothing` helpers and their sidebar-only imports were removed (the per-model usage table still exists for `/gentle:usage` and the compact bar via `renderShellBar`, both untouched).
  - RED (header bar, pure): `SyntaxError: The requested module '../lib/shell-bar.ts' does not provide an export named 'buildShellHeaderModel'`, then per-behavior assertion failures (drop order, right-alignment, "" fallback) in tests/shell-bar.test.ts.
  - RED (Status slimming): `AssertionError: The input was expected to not match the regular expression /Usage/` in tests/shell-bar.test.ts.
  - RED (sidebarHeader): `SyntaxError: ... does not provide an export named 'sidebarHeader'` in tests/shell-sidebar.test.ts.
  - RED (layout vstack): `AssertionError: 'hstack' !== 'vstack'` and an empty header-lines array where `['HEADER one']` was expected, in tests/shell-sidebar-layout.test.ts.
  - RED (gentle-shell wiring): `TypeError: Cannot read properties of undefined (reading 'digest')` (no "header" part registered yet) in tests/gentle-shell.test.ts.
  - GREEN: `node --experimental-strip-types --test tests/shell-bar.test.ts tests/shell-sidebar.test.ts tests/shell-sidebar-layout.test.ts tests/gentle-shell.test.ts tests/shell-sidebar-banner.test.ts` → 104/104 pass.
  - `pnpm run typecheck` → no regressions (197 recorded diagnostics, 2 improved). `pnpm test` → 2700 pass / 0 fail / 38 skipped (pre-existing platform skips), exit 0.
  - Deviation: mid-T2, the user edited this spec file to drop Agents from the sidebar rail entirely (new T5, inserted before T4) and to revert the Agents PANEL-tone option T1 had added. Handled inline (see T1 addendum above); T5 itself is queued after T3.

- T3 (sha: pending commit) — per-section render memo in lib/shell-sidebar-layout.ts: a new `sectionCache: Map<string, SectionCacheEntry>` inside `installSidebar` caches each of the three rail sections' rendered lines keyed by (component identity, digest, contentWidth, theme); a section with its own digest is revalidated by that digest alone (a sibling's ticking header/Status digest never forces it to re-render), a section with no digest (or a throwing one) falls back to the shared revision counter exactly like the old whole-rail memo did. The existing whole-rail `prepared` fast path is untouched as the zero-work path for "literally nothing changed"; the section cache only matters once that fast path misses.
  - RED: `AssertionError: only the section whose digest changed re-renders` — `{footer:2, agents:2, todo:2}` where `{footer:2, agents:1, todo:1}` was expected, in tests/shell-sidebar-layout.test.ts.
  - GREEN: `node --experimental-strip-types --test tests/shell-sidebar-layout.test.ts tests/shell-sidebar.test.ts tests/shell-bar.test.ts tests/gentle-shell.test.ts tests/gentle-todo.test.ts tests/gentle-agents.test.ts` → 212/212 pass.
  - `pnpm run typecheck` → no regressions (197 recorded diagnostics, 2 improved).
  - Behavior change (not a regression): an existing test ("real layout frames reuse unchanged sidebar output...") expected a full 3-section re-render merely from dipping below the 140-col breakpoint and back, or toggling fullscreen/regular mode, because the old whole-rail memo got nulled on every inactive frame. The per-section cache survives that dip (nothing in the actual render inputs changed), so those renders are now correctly skipped; updated the test's expected counts with a comment explaining why.
  - Deviation: mid-T3, the user added a further T6 ("Rose frame for every INFO card") after T5 and before T4, and updated the acceptance criteria to match (every INFO card gets the border/accent frame; the Agents widget stays above the editor with that frame). Queued after T5, before T4.

- T5 (sha: pending commit) — extensions/gentle-agents.ts's above-editor widget factory now returns a plain `{render, invalidate}` component instead of `sidebarPart(tui, "agents", ...)`, so it never claims a rail slot and is never subject to the fullscreen sidebar's bottom-suppression (`state.active && state.ownsHost?.()`); the removed second ("rail") render variant with it. lib/agents-widget.ts: `meta()` (one joined, per-row-filtered string) replaced by `metaFields`/`metaColumnWidths`/`metaRowText`/`metaTotalWidth`, giving model·effort, tokens, cost, and elapsed each a fixed width (the widest value among shown tasks) and right-aligning every row's value inside it — a queued row now leaves model/tokens/cost blank (padded) rather than the "queued" text overwriting the whole block, and columns line up vertically even when one row has short values and another has long ones. `narrowMetaText()` keeps the old single-string contract for the narrow (non-fullMetrics) degradation, which T5 explicitly kept unchanged. Left `lib/shell-sidebar-layout.ts`'s hardcoded `["footer", "agents", "todo"]` section list untouched (out of T5's stated file scope) — "agents" now permanently resolves to `state.parts.get("agents") === undefined` and is filtered out of the rail, which is the acceptance criterion in practice.
  - RED (rail removal): `AssertionError: Agents never claims a rail slot... true !== false` in tests/gentle-agents.test.ts.
  - RED (column alignment): `AssertionError: the tokens column ends at the same offset on every row — 84 !== 83` in tests/agents-widget.test.ts.
  - GREEN: `node --experimental-strip-types --test tests/agents-widget.test.ts tests/gentle-agents.test.ts tests/shell-sidebar-layout.test.ts tests/shell-sidebar.test.ts tests/gentle-shell.test.ts` → 188/188 pass.
  - `pnpm run typecheck` → no regressions (197 recorded diagnostics, 2 improved).
  - Deviation: mid-T5, the user added T7 ("Reclaim the empty footer row in fullscreen") after T6 and before T4, with a note that they are running this branch live and every commit must stay green. Queued after T6, before T4.

- T6 (sha: pending commit) — lib/shell-card.ts: `FRAME_ROLE[INFO]`/`TITLE_ROLE[INFO]` changed from `customMessageLabel`/`customMessageLabel` to `border`/`accent`, making INFO byte-identical to PANEL; deleted `CARD_TONE.PANEL` and its two role-map entries per T6's explicit "collapse" instruction, and reverted T1's two call sites (lib/shell-bar.ts Status card, lib/shell-todo.ts Todos card's `options.scrollable` ternary) back to plain `CARD_TONE.INFO`. No other call site needed a change: the review preflight card (extensions/gentle-shell.ts) and the quiet-state Agents widget (lib/agents-widget.ts `tone()`) already used `CARD_TONE.INFO` and inherit the rose frame automatically.
  - RED: `AssertionError: The input did not match the regular expression /^<border>╭<\/border>.../` — input still `<customMessageLabel>╭</customMessageLabel>...` — in tests/shell-card.test.ts.
  - GREEN: `node --experimental-strip-types --test tests/shell-card.test.ts tests/shell-bar.test.ts tests/shell-todo.test.ts tests/agents-widget.test.ts tests/gentle-shell.test.ts tests/gentle-todo.test.ts tests/gentle-agents.test.ts tests/shell-sidebar-layout.test.ts tests/shell-sidebar.test.ts tests/shell-sidebar-banner.test.ts` → 255/255 pass.
  - `pnpm run typecheck` → no regressions (197 recorded diagnostics, 2 improved). `pnpm test` → 2707 pass / 0 fail / 38 skipped (pre-existing platform skips), exit 0.

- T7 (sha: pending commit) — lib/shell-sidebar-layout.ts: new `reclaimFooterRow(node)` walks the root's node's last entry (Pi's dock, chat-viewport.js), calls its own `[NODE]()`, and — only if the dock's last entry (the footer) has `minSize > 0` — returns a rebuilt root node whose dock entry points at a new wrapper component exposing a rebuilt dock node with that footer entry's `minSize` set to 0; every untouched entry/object is reused by reference (only the two touched objects are shallow-copied), so Pi's real dock/root objects are never mutated. Wired into `left`'s `[NODE]` (`() => reclaimFooterRow(original.call(root))`) — the transcript+dock branch that's only ever consulted while the sidebar is active; the plain `original.call(root)` return path used when inactive is untouched, so the reserved row stays exactly 1 there (correct, since the footer isn't suppressed then).
  - RED: `AssertionError: the footer entry's reserved row is collapsed while the sidebar paints — 1 !== 0` in tests/shell-sidebar-layout.test.ts.
  - GREEN: `node --experimental-strip-types --test tests/shell-sidebar-layout.test.ts tests/shell-sidebar.test.ts tests/gentle-shell.test.ts` → 79/79 pass (includes the real-pi-tui-VStack integration test `fullscreen native transcript renders once...`, whose simplified two-entry dock has no nested `[NODE]`, so `reclaimFooterRow` no-ops there by design — it only activates on the real chat-viewport dock shape).
  - `pnpm run typecheck` → no regressions (197 recorded diagnostics, 2 improved). `pnpm test` → 2708 pass / 0 fail / 38 skipped (pre-existing platform skips), exit 0.
  - Deviation: mid-T7, the user added a further T8 ("Usage in the header, with shortcut and click") after T7 and before T4, with matching acceptance-criteria and delivery-strategy updates. Not started; reported to the parent rather than assumed.

## Next step
T8 (usage segment with shortcut and click in the header — added mid-implementation, not yet started) or T4 (docs), pending confirmation from the parent on whether T8 is in scope for this writer pass.
