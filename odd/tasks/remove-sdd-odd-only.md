# Remove SDD; retain ODD only (Gentle Shell)

## Objective and problem
Gentle Shell still exposes SDD through palette, extension commands, agents, native integration, setup and docs. Remove this alternate workflow so ODD is the sole development workflow. The user explicitly authorized deletion of historical OpenSpec/SDD files from the current tree; Git history remains intact.

## Scope and constraints
- Repository: Gentle Shell (`gentle-pi` source), isolated feature worktree `feat/remove-sdd-odd-only` from local `main` (ef55af79).
- Preserve ODD, RDD/review integration, unrelated user state and the original checkout. Align native consumer with gentle-ai provider retirement.
- No remote operations, publishing, or rewriting Git history. User authorized local Conventional work-unit commits on isolated feature branches; push and PR creation remain unauthorized.
- Implementation route: delegated direct (multi-file); read-only mapping completed. Delivery strategy: single-pr with user-approved size exception; forecast greatly exceeds 400 authored changed lines. This does not authorize creating the PR.
- TDD: enabled by the original `openspec/config.yaml` (`strict_tdd: true`); capture RED/GREEN/REFACTOR for changed behavior. The linked dependency worktree cannot run `pnpm typecheck` without attempting a package reinstall; use the direct Node test runner and `node scripts/check-types.mjs` baseline gate instead. Removing the SDD configuration does not erase its recorded source.
- ~400 changed lines/task is only an advisory planning heuristic; keep related code/tests/docs together.

## Tasks
- [x] SHELL-1: Remove SDD command palette, TUI/startup/status, extension commands, preflight and child dispatch/native SDD consumer while preserving ODD and review. Route: delegated writer (multi-file). Acceptance: no SDD commands register or appear in palette; focused tests/typecheck pass. Work-unit commit `cc5fbd94b8830a58cbc32ae7ddcb1be3762b6243`.
- [x] SHELL-2: Remove packaged SDD agents/chains/workflow/support assets, setup/manifest expectations, docs and SDD-only tests. Route: delegated writer (multi-file). Acceptance: offline package builds without SDD assets and docs describe ODD only. Work-unit commit `cc5fbd94b8830a58cbc32ae7ddcb1be3762b6243`; the separate full packed-runner installation passed after explicit permission for isolated anonymous downloads, as recorded below.
- [x] SHELL-3: Delete historical OpenSpec/SDD files from current tree; retain the one historical migration-test fixture in source to prove edited user files survive, but exclude it from the npm tarball. Independent review coverage remains. Route: delegated writer (multi-file). Acceptance: retained tests are relevant and residual references are explained. Work-unit commit `cc5fbd94b8830a58cbc32ae7ddcb1be3762b6243`.
- [ ] SHELL-4: Run applicable offline full test/type-baseline/package checks and coordinate provider/consumer compatibility; report failures/skips. Route: delegated verifier. Local checks and full packed-runner installation passed; native review and live-agent journeys remain open.

## Progress and evidence
- Read-only mapper identified TUI, extensions, native SDD status binding, installer, assets, docs/tests and 164 tracked OpenSpec files.
- Historical deletion choice: user selected deletion in both repos. Parent enumerated and removed exactly 164 tracked regular `openspec/**` files in this isolated worktree (312 in gentle-ai's worktree); Git history is untouched. References/tests/docs remain to migrate, so SHELL-3 is not complete.
- SHELL-1 partial: public SDD palette and command registrations were removed while delegation/review commands and ODD/RDD remained. SDD-only `extensions/sdd-init.ts` and `tests/sdd-preflight.test.ts` were enumerated and deleted by parent; package verifier, palette tests and runtime inventory no longer require them. `node scripts/verify-package-files.mjs` passed; palette + package-manifest tests passed (85/85). A focused palette verifier first found an overbroad assertion rejecting dynamic delegation/review installation commands; corrected test passed (34/34). The runtime harness initially failed on obsolete SDD expectations; bounded migrations retired SDD-only prompt/status/RPC, preflight/init and asset fixtures, narrowed the mixed installer loop to delegation/review, and preserved doctor/Engram, ordinary model routing, primary ODD/RDD, and review checks. `node --experimental-strip-types tests/runtime-harness.mjs` now passes (exit 0). Generic `legacyModelsCwd` still uses an SDD-named fixture and needs migration during residual audit. User clarified the observed error occurs when launching `gentle-shell` in a terminal, not direct `pi`; read-only trace located an undefined `readCommandSddStatus` call in SDD-phase startup in `extensions/gentle-ai.ts`, which the terminal launcher loads. A bounded writer removed that stale SDD startup branch and its obsolete status/continue harness scenario, preserving primary ODD/RDD and named-agent behavior; focused palette tests pass (34/34). After retiring remaining SDD-only harness scenarios, full runtime harness passes (exit 0); this does not prove direct terminal launch. The user's exact launch was not reproduced. No commits; SHELL-1 remains open.
- Verification environment: isolated `node_modules` is a symlink to the unchanged main checkout dependency tree (same package.json); direct Node tests resolve dependencies. `pnpm typecheck` attempted a pnpm install on the symlink and aborted with `ERR_PNPM_ABORTED_REMOVE_MODULES_DIR_NO_TTY` before TypeScript compilation. Independent verifier then ran `node scripts/check-types.mjs` directly: `tsc -p tsconfig.json` did compile with `noEmit: true`; the repository's baseline gate passed with 192 existing diagnostics (not a clean compile), six file/code pairs improved and no regressions. The same verifier passed package inventory (170 files, 69 byte-pinned artifacts), palette+manifest (85/85), and runtime harness (exit 0).
- SHELL-1 second dispatch slice: removed SDD-only selection, research, and remediation from `extensions/gentle-agents.ts`; retired `sdd-*` agent names and removed selectors fail closed before child launch, including continuation. The parent deleted three inspected SDD-only test files; the mixed `gentle-agents.test.ts` suite retains generic named-agent, foreign worktree, and review delegation tests and now checks retired child envelopes deny tools. Strict TDD RED: the focused suite initially failed 10 obsolete SDD cases after the parent deletions; GREEN: all 115 focused tests passed, runtime harness exited 0, and direct type baseline passed with 192 recorded diagnostics/no regressions (six file/code pairs improved). No commit; SHELL-1 remains open pending residual integration.
- SHELL-2 extraction slice: moved package-owned asset catalog, SHA manifest, process lock, legacy adoption, model/thinking preservation, selective refresh and retirement into `lib/agent-assets.ts`; retained compatibility exports from `lib/sdd-preflight.ts` and temporarily retained the SDD catalog. Production extension and principal package tests now import the independent owner. ODD strict-TDD support files remain packaged; migration manifests unchanged. Strict TDD RED: package-manifest import of the new owner failed `ERR_MODULE_NOT_FOUND`; GREEN: focused suites 131/131 pass, package inventory passes (171 files), direct baseline type check reports 190 recorded diagnostics/no regressions. Existing SDK discovery test initially failed because the previously deleted `extensions/sdd-init.ts` was still imported by its fixture; adjusted it to assert currently supported delegation/review discovery without SDD activation. No commits; SHELL-2 remains open for later SDD retirement.
- SHELL-1/2 later slices: removed SDD preflight/selection/research hooks and fail-closed child dispatch, extracted generic asset installation into `lib/agent-assets.ts`, retired SDD active asset catalog while preserving hash-proven retirement of installed SDD files and moving strict-TDD support ownership to delegation. Parent guarded-deleted 18 packaged SDD assets (13 agents, 3 chains, 2 docs) plus pure-SDD preflight/status/research/OpenSpec delta modules and dedicated tests; historical `openspec/**` removal remains current-tree only. Native CLI SDD status/continue/types/decoder removed from source and generated runtime mirror; generator check passes, only `runtime/native-review-cli.mjs` changed among seven authorized outputs. The four mixed orchestrator assets and three generic agent assets plus strict-TDD guidance were rewritten ODD-only; mixed tests retain RDD, telemetry, model, language, research and delegation coverage. Public docs, package description/keywords and two architecture/telemetry docs now describe ODD rather than SDD. ODD-only integration test replaced the mixed SDD test; dedicated SDD tests were guarded-deleted after inspecting their coverage. No local commit yet; SHELL-1/SHELL-2 remain open for residual audit and coherent work-unit boundary.
- SHELL-4 integration evidence (before any later edits): full direct Node suite `node --experimental-strip-types --test tests/*.test.ts`: 3,223 tests, 3,180 passed, 0 failed, 43 skipped (including Windows-native checks). Runtime harness, provider contract 1.2.0, seven generated module drift checks, 152-file package inventory and direct `node scripts/check-types.mjs` baseline gate passed; type gate reported 188 existing diagnostics/no regressions, NOT a diagnostic-free compile. `pnpm typecheck` previously tried installing on the ignored `node_modules` symlink and aborted before compilation, so direct Node script is the valid observed type check. Full packed-package and terminal launch remain unverified; issue #1345 repository identity was requested but not yet confirmed. No push/PR/remote access.
- Engram mirror pending: this Pi session belongs to `gentle-ai`, and Engram rejected a `gentle-pi`-scoped save. Keep this local file authoritative until a Gentle Shell session can mirror it.

- SHELL-1/2/3 final evidence: removed orphaned SDD launch fields from `lib/agents-runner.ts` while retaining legacy task-history pruning safeguards. Retired `quiet-tools` recognition of SDD commands and replaced the startup SDD-phase count with non-SDD agent count. Three clean, unreferenced SDD files were guardedly deleted; the historical `sdd-research-v2.5.0.md` fixture remains only in source for exact-hash and edited-copy preservation tests. `package.json` excludes precisely that fixture from distribution, with an offline npm-pack regression test.
- SHELL-4 current checks after final whitespace normalization: direct suite **3,172 passed, 0 failed, 43 skipped**; changed test files reran **17/17**. Runtime harness, provider contract 1.2.0, seven-module drift check, 152-resource inventory (69 byte-pinned), cross-lane closure parity and `git diff --check` passed. `node scripts/check-types.mjs` passed its baseline gate with **188 recorded diagnostics and no regressions**, not a diagnostic-free compile. Offline `npm pack --ignore-scripts --offline` contained **507 files with no SDD/OpenSpec-named paths**. The prior actual packed `gentle-shell` launcher (Pi 0.87.1, isolated home, `--no-approve`) reached the interactive TUI without `readCommandSddStatus` or extension-load errors, but its graceful exit was not proven. Full packed-package `scripts/test-packed-runner.mjs` performs an isolated npm install of pinned Pi SDK 0.85.1; the fresh cache cannot provide that dependency offline, and remote npm was not authorized. It remains explicitly unverified. No npm install, remote operation, commit, push or PR was performed.

- **Final provider compatibility:** gentle-ai's normalized post-AI-5 Go gate passed (76 packages, 4 skipped, 0 failing leaves); `go vet` passed, and offline organic-runtime E2E passed 20/0/5 with real-agent transports skipped. Shell's cross-lane closure parity and provider contract 1.2.0 passed. These checks do not substitute for the unverified packed-runner install or live external journeys.

- **Fresh independent verification:** `node --experimental-strip-types --test tests/asset-installation-runtime.test.ts tests/odd-routing-contract.test.ts tests/gentle-ai.test.ts` passed **93/93**, with no failures or skips. `node scripts/check-types.mjs` passed its baseline gate with **188 pre-existing diagnostics and no regressions**, not a clean TypeScript compile. `node scripts/verify-package-files.mjs` passed for **152 required resources and 69 byte-pinned artifacts**. Independent structural inspection found no actionable issue; commit-range `git diff --check` passed and the final working-tree status was unchanged. This did not rerun the full 3172/43 suite or rebuild the 507-file tarball recorded above.
- **Native committed-range review blocker:** Assessment identified a high-risk candidate with 260 paths and 31,072 additions plus deletions. Native START stopped at `lens_context_budget_exceeded` before creating review authority (`not_started`); there is no lineage or receipt. Native guidance requires smaller reviewable candidates; the existing coherent work-unit commit has not been rewritten, and the user explicitly chose to preserve the current commits. SHELL-4 remains open for native review and unverified live external journeys.
- **Authorized packed install proof:** After the user authorized only this external proof, `node scripts/test-packed-runner.mjs` ran with `env -i`, isolated HOME, npm cache and npm configuration, anonymous npm registry and the pinned Gentle AI v3.6.1 release download. It exited 0: **packed package E2E passed (gentle-pi 3.6.0; Gentle AI 3.6.1)**; npm added 65 packages. The temporary root was removed and the worktree remained clean. This proves the packed consumer with the pinned published native binary, not the unmerged gentle-ai source commit, live-agent journeys, a graceful interactive terminal exit or an RDD receipt. npm's `always-auth` warning was non-failing.
- **Local delivery and rollback:** Coherent work-unit commit `cc5fbd94b8830a58cbc32ae7ddcb1be3762b6243` (`refactor(shell): remove SDD surfaces and retain user safety`) closes SHELL-1–SHELL-3 together because consumer retirement, tests and assets could not form separate working packages. Cross-repository rollback means reverting this commit together with gentle-ai commit `a20da5a0ce2f7075597447bf3bb46b8b622c997a`. Source whitespace was normalized before the final repeated full Shell suite (3172 pass, 43 skip), provider check, runtime harness, type baseline and offline 507-file pack; all passed. No push or PR was made.

## Next step
Keep the verified local commits intact as the user requested; no native receipt exists. The full packed-runner install passed, while live external journeys remain unverified and are not covered by the packed-install authorization. Preserve hash-proven retirement and modified-user-file safety; no history rewrite, push or PR is authorized. Local Engram mirror remains pending because this Pi session belongs to `gentle-ai`; do not claim a mirror was saved.
